{
  "name": "php-react-example",
  "version": "0.0.0",
  "main": "index.js",
  "repository": "ssh://git@github.com/WillBooster/php-react-example.git",
  "author": "WillBooster Inc.",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "prestart": "mkdir dist && cp src/index.php dist",
    "start": "concurrently \"esbuild src/index.jsx --bundle --outdir=dist --watch\" \"php -S localhost:3000 -t dist\""
  },
  "dependencies": {
    "esbuild": "0.14.25",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "concurrently": "7.0.0"
  }
}
